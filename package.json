{
  "name": "cappuccino-cafe-languages",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "node src/sync-translations.mjs"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.5.9"
  }
}
